generate_projects:
  blocks:
    - block_name: demo-prod
      root_dir: "terragrunt/projects/demo/prod"
      terragrunt: true
      workflow: default

    - block_name: demo-uat
      root_dir: "terragrunt/projects/demo/uat"
      terragrunt: true
      workflow: default

workflows:
  default:
    workflow_configuration:
      on_pull_request_closed: ["digger unlock"]
      on_commit_to_default: ["digger unlock"]

    plan:
      steps:
      - init:
      - plan:
    apply:
      steps:
      - init:
      - apply:
